"""App configuration for camera_line_annotator."""

from django.apps import AppConfig


class CameraLineAnnotatorConfig(AppConfig):
    """Configuration for camera line annotator app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "camera_line_annotator"
